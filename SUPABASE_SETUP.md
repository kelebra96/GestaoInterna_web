# Supabase Setup

Este guia de configuração para ambiente local (self-hosted com Docker) foi descontinuado.

A aplicação agora utiliza **Supabase Cloud**.

Para configurar o ambiente de desenvolvimento, conecte-se diretamente ao projeto na nuvem da Supabase. As variáveis de ambiente necessárias estão no arquivo `.env` do projeto.